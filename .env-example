# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Filler Words (comma-separated list)
# Example: FILLER_WORDS=um,uh,like,you know,actually,basically,literally
FILLER_WORDS=um,uh,like,you know,actually,basically,literally

# Allowed User Handles (comma-separated, with or without @)
# Leave empty to allow all users
# Example: ALLOWED_HANDLES=@user1,user2,@user3
ALLOWED_HANDLES=

# Admin Handles (comma-separated, with or without @)
# Admins can start/stop the bot
# Leave empty to allow all users to manage the bot
# Example: ADMIN_HANDLES=@admin1,admin2
ADMIN_HANDLES=

# Database Path (optional, defaults to filler_words.db)
DATABASE_PATH=filler_words.db
